import { LuMessagesSquare } from "react-icons/lu";

const SVG_Comment: React.FC = () => {
    return <LuMessagesSquare className="text-2xl flex-shrink-0 items-end" />;
};

export default SVG_Comment;
