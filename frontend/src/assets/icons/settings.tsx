import { IoSettingsOutline } from "react-icons/io5";

const SVG_Settings: React.FC = () => {
    return <IoSettingsOutline className="text-2xl flex-shrink-0 items-end" />;
};

export default SVG_Settings;